# Contributors

* **Radoslav Ďurač** ([@rraddatch](https://github.com/rraddatch)) — Slovak (`sk`) UI translation, Slovak demo (`demo-sk.html`), Slovak captions/transcript for the "About OzPlayer" video, and a fix for the control-button click handler collision with Chromium's native `HTMLButtonElement.command` (Invoker Commands API). See `changelogs/OzPlayer-4.2-changelog.md`.
